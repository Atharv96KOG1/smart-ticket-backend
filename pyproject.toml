[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]

[tool.ruff.lint.per-file-ignores]
"src/smart_ticket_router/main.py" = ["E402"]  # load_dotenv() must run before imports that read env vars
